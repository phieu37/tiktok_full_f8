// config cấu hình (định nghĩa tuyến đường)
const routes = {
    home: '/',
    following: '/following',
    profile: '/:nickname',
    upload: '/upload',
    search: '/search',
};

export default routes;
